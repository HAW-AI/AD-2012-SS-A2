package a2;

import java.util.Observable;

public class Timer extends Observable {
    //public void step();
    
    //bei jedem Step wird notifyObservers() aufgerufen und der aktuelle stepcount übergeben.
}
